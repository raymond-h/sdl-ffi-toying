{
  "name": "sdl-ffi-toying",
  "description": "Toying with SDL2 via FFI",
  "version": "0.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "author": "Raymond Hammarling",
  "repository": "raymond-h/sdl-ffi-toying",
  "scripts": {
    "lint": "eslint src/ test/",
    "pretest": "npm run lint",
    "watch": "onchange src/ -- npm run build && echo Done",
    "dev": "npm run watch",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "canvas": "^1.3.13",
    "ffi": "^2.0.0",
    "lodash": "^4.11.2",
    "ref": "^1.3.2",
    "ref-struct": "^1.0.2",
    "ref-union": "^1.0.1"
  }
}
